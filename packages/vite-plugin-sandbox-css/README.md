# vite-plugin-sandbox-css

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build vite-plugin-sandbox-css` to build the library.

## Running unit tests

Run `nx test vite-plugin-sandbox-css` to execute the unit tests via [Vitest](https://vitest.dev/).
