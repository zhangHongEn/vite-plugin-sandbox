import { moduleFederationVite } from './module-federation-vite';

describe('moduleFederationVite', () => {
  it('should work', () => {
    expect(moduleFederationVite()).toEqual('module-federation-vite');
  });
});
