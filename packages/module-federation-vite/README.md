# module-federation-vite

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build module-federation-vite` to build the library.

## Running unit tests

Run `nx test module-federation-vite` to execute the unit tests via [Vitest](https://vitest.dev/).
